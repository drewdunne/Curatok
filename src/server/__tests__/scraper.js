const scraper = require('../../scraper/scraper.js').videos;

describe('scraper tests', () => {
    it('receives an object with two properties in each entry', async () => {
        
        scraper('drewdunnehere', );
        // const result = await scraper('drewdunnehere', 800);
        // expect(Object.keys(Object.entries(result)[0])).toHaveLength(2);
        // }, 60000)
    });
});
